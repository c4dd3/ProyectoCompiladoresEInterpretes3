PROGRAM Prueba

VAR
  numero, limite, contador, a, i: INT;
  nombre, apellido: STRING;

BEGIN
  numero := 1;
  limite := 2;
  contador := numero + limite;
  a := 5;
  i := 0;

  a := a + 1;
  a := a - 1;

  WRITE("Fin");
  WRITE(a);
END
